export const environment = {
  production: false,
  appConfig: "dev",
  pathConfig: "./assets/config",
  apiSuffix: "/api/v1/",
  apiUrl: "https://localhost:5051"
};

export const resources = {
  auth: "auths",
  subjects: "subjects",
  student: "students"
};
